<?php

namespace app\model;

use app\core\Model;
use Exception;

class HandleSchoolModel extends Model{

    private $db;

    public function __construct(){

        $connection = $this->connect();

    $this->db = $connection->initializeQuerybuilder("progress", "id");
    }
}

        public function fetchById(string $id){   
        try {
        $progress = $this->db->findBy("id", $id)->getResult();
        return $progress;
    
            }
        }
console.log("Message here");

    